{{> topPage }}

<div class="vh-100 d-flex flex-column">
    {{> navbar }}

    <div class="main">
        <div class="container p-3">
            <div class="d-flex align-items-start">
                <div class="nav flex-column nav-pills me-3" id="v-pills-tab" role="tablist" aria-orientation="vertical">
                    <button class="nav-link active" data-bs-toggle="pill" data-bs-target="#pill-post" type="button" role="tab" aria-controls="pill-post" aria-selected="true">Posts</button>
                </div>
                <div class="tab-content" id="v-pills-tabContent">
                    <div class="tab-pane fade show active" id="pill-post" role="tabpanel" aria-labelledby="v-pills-home-tab">
                        <p>Posts</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>

{{> bottomPage }}